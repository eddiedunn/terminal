# Ansible managed: {{ ansible_managed }}
# Initialization for {{ tool.name }}

{% if tool.env_vars is defined %}
# Environment variables for {{ tool.name }}
{% for var_name, var_value in tool.env_vars.items() %}
export {{ var_name }}="{{ var_value }}"
{% endfor %}
{% endif %}

{% if tool.aliases is defined %}
# Aliases for {{ tool.name }}
{% for alias_name, alias_cmd in tool.aliases.items() %}
alias {{ alias_name }}='{{ alias_cmd }}'
{% endfor %}
{% endif %}

{{ current_init_snippet }}
