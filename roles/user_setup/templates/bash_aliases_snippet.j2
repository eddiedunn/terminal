{# Ansible managed: {{ ansible_managed }} #}
# This file contains shell aliases and is automatically generated.

# --- Tool-Specific Aliases ---
{% for tool in terminal_setup_tools_processed %}
{%- if (tool.state | default('present')) == 'present' and tool.aliases is defined and tool.aliases %}
# {{ tool.name | title }} Aliases
{% for alias_name, alias_cmd in tool.aliases.items() %}
alias {{ alias_name }}='{{ alias_cmd }}'
{% endfor %}
{%- endif %}
{%- endfor %}

# --- Custom User Aliases ---
{% if terminal_setup_custom_aliases is defined and terminal_setup_custom_aliases %}
# User-defined ad-hoc aliases from playbook
{% for alias_name, alias_cmd in terminal_setup_custom_aliases.items() %}
alias {{ alias_name }}='{{ alias_cmd }}'
{% endfor %}
{% endif %}

# --- Custom User Aliases ---
{% if terminal_setup_custom_aliases is defined and terminal_setup_custom_aliases %}
# User-defined ad-hoc aliases
{% for alias_name, alias_cmd in terminal_setup_custom_aliases.items() %}
alias {{ alias_name }}='{{ alias_cmd }}'
{% endfor %}
{% endif %}
